// Motion tokens — introduced with the TabBar sliding indicator.
export const motion = {
  durationMd: '250ms', // standard UI transition
  easeStandard: 'cubic-bezier(0.4, 0, 0.2, 1)', // fast start, soft stop
};
